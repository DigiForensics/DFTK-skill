# Example — PCAP endpoint

Question: “Which domain did the client contact?”

Depending on protocol/encryption, useful evidence may be DNS, HTTP Host, or TLS SNI. Correlate time/flow context when attribution to a specific client/process matters. A partial capture with no hit cannot prove no contact occurred.
