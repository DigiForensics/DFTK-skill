# Disk images / E01

Filesystem/image parsing may depend on specialist `pyewf` / `pytsk3` environments. Missing bindings produce a capability limitation, not evidence absence.

Preserve image identity/hash and distinguish image-level metadata, filesystem metadata, allocated files, deleted entries, and derived extracted copies. Use filesystem times according to their specific semantics.
