# Email

Preserve raw message/header provenance. Distinguish message fields supplied by a sender from authentication results produced by receiving infrastructure.

DKIM/SPF/DNS checks may require optional dependencies and sometimes network authorization. `unsupported` or blocked network access cannot be interpreted as authentication failure.
