{
  "mcpServers": {
    "dftk": {
      "command": "dftk",
      "args": [
        "mcp",
        "--root", "/abs/path/to/evidence",
        "--workspace", "/abs/path/to/case-workspace",
        "--max-safety", "READ_ONLY",
        "--timeout", "180"
      ]
    }
  }
}
